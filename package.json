{
  "name": "russmedia-hooks",
  "version": "1.0.0",
  "description": "Mimics the basic functionality of WordPress hooks, but with support for async operations.",
  "main": "build/main/index.js",
  "typings": "build/main/index.d.ts",
  "module": "build/module/index.js",
  "repository": "https://gitbub.com/russmediadigital/russmedia-hooks",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.json",
    "build:module": "tsc -p tsconfig.module.json",
    "dev": "tsc -p tsconfig.json -w"
  },
  "author": "Martin Widmann",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.3",
    "typescript": "^4.0.5"
  }
}
